# Optional local overrides for Serverless deploy (not required today).
# Lambda env (PHOTOS_TABLE, PHOTOS_BUCKET, etc.) is defined in serverless.yml
# and injected by CloudFormation — not from this file.
#
# AWS_REGION=us-east-1
# SLS_STAGE=dev
